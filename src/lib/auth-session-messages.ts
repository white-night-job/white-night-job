/** Shown when an authenticated session has expired and the user must sign in again. */
export const SESSION_EXPIRED_MESSAGE =
  "ログインの有効期限が切れました。再ログインしてください。";

/** Generic unauthenticated response (e.g. never logged in). */
export const UNAUTHENTICATED_MESSAGE = "ログインしてください。";
